import { AuthenticateUserDto } from './authenticate-user.dto';

describe('AuthenticateUserDto', () => {
  it('should be defined', () => {
    expect(new AuthenticateUserDto()).toBeDefined();
  });
});
